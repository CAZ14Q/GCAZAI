export default async function handler(req, res) {
  if (req.method !== 'POST') {
    return res.status(405).json({ error: 'Method not allowed' });
  }

  const { message, system } = req.body;

  // محاكاة رد ذكي (مكان OpenAI أو أي AI)
  const reply = `
تم استلام سؤالك بنجاح.

🔬 التحليل الباطني:
يرجى توضيح الأعراض السريرية.

🩺 التقييم الجراحي:
لا توجد مؤشرات جراحية عاجلة حالياً.

🧬 التحليل الجيني:
ينصح بتحليل المتغيرات الوراثية المرتبطة بالحالة.

(هذا رد تجريبي من G CAZ AI)
`;

  res.status(200).json({ reply });
}
